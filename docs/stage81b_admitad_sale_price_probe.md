# Stage 81b: Admitad sale_price probe

Dry-run diagnostic for Admitad product feeds. It checks whether Google-style `price` + `sale_price` fields can form a real discount pair.

No import, no delete, no production changes.

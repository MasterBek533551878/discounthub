# Provider templates

These files are templates for configuring real official feeds.

Copy one provider object into:

`backend/config/feed_providers.json`

Then update `id`, `name`, `url`, and `adapter`.

Never commit production secrets or private feed tokens into this repository.
